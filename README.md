# 🎓 Дипломный проект по автоматизации тестирования

> Проект выполнен в рамках курса **QA.GURU** по автоматизации тестирования.  
> Реализованы UI и API тесты с использованием **Playwright** на **JavaScript**.

| Тип тестов | Тестируемый сервис |
|---|---|
| UI | [realworld.qa.guru](https://realworld.qa.guru/) |
| API | [apichallenges.herokuapp.com](https://apichallenges.herokuapp.com) |

---

## 🛠 Технологии и инструменты

| Инструмент | Назначение |
|---|---|
| [Playwright](https://playwright.dev/) | UI и API автоматизация |
| JavaScript | Язык тестов |
| GitHub Actions | CI/CD-пайплайн |
| Docker | Запуск в контейнере |
| Allure (опционально) | Отчётность |

---

## 📁 Структура проекта

```
diplom_qa.guru_automation/
├── .github/workflows/        # CI/CD пайплайн (GitHub Actions)
├── src/                      # Вспомогательный код (хелперы, данные и т.д.)
├── tests/                    # Тестовые сценарии
├── playwright.config.js      # Конфигурация Playwright
├── Dockerfile                # Docker-образ для запуска тестов
└── package.json              # Зависимости проекта
```

---

## 🚀 Запуск тестов

### Локально

**1. Клонировать репозиторий**
```bash
git clone https://github.com/HelenBelova17/diplom_qa.guru_automation.git
cd diplom_qa.guru_automation
```

**2. Установить зависимости**
```bash
npm install
npx playwright install
```

**3. Запустить тесты**
```bash
# Все тесты
npx playwright test

# В headed-режиме (с браузером)
npx playwright test --headed

# Конкретный файл
npx playwright test tests/example.spec.js
```

---

### В Docker

```bash
# Собрать образ
docker build -t playwright-tests .

# Запустить тесты
docker run --rm playwright-tests
```

---

### Через GitHub Actions

Тесты запускаются автоматически при каждом `push` и `pull request` в ветку `main`.  
Конфигурация пайплайна находится в `.github/workflows/`.

---

## 📊 Отчёты о тестировании

### Playwright Report

После локального прогона открыть встроенный HTML-отчёт:

```bash
npx playwright show-report
```

### Allure Report

**Локально:**

```bash
# Запустить тесты с генерацией allure-results
npx playwright test

# Сгенерировать и открыть отчёт
npx allure generate allure-results --clean -o allure-report
npx allure open allure-report
```

**В CI (GitHub Actions)** пайплайн автоматически:

1. Запускает тесты и собирает `allure-results/`
2. Генерирует отчёт в папку `allure-report/`
